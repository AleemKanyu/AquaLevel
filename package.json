{
  "name": "aqualevel",
  "version": "1.3.0",
  "description": "Smart Water Level Monitoring System",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp *.png www/ 2>/dev/null || true",
    "sync": "npm run build && cap sync",
    "cap:sync": "npm run build && cap sync ios"
  },
  "keywords": [
    "aqualevel",
    "water-monitoring",
    "ios",
    "capacitor"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
